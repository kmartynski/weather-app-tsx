import * as React from 'react';
import styled from '@emotion/styled';


const Container = styled.div`

`


export const EmptyState: React.FC = (): React.ReactElement => {
    return (<Container>Empty State</Container>)
}